# Инструкция по запуску сервера

## Проблема: сервер перестал работать

Если сервер перестал отвечать или показывает черный экран:

### 1. Остановите все процессы Python

В PowerShell выполните:
```powershell
Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force
```

Или закройте все окна с запущенным сервером.

### 2. Проверьте, что порт 5000 свободен

```powershell
netstat -ano | findstr ":5000"
```

Если порт занят, найдите процесс и остановите его:
```powershell
Stop-Process -Id <PID> -Force
```

### 3. Запустите сервер заново

**Важно:** Запускайте сервер из директории проекта!

#### Способ 1: Через командную строку (рекомендуется)

1. Откройте **Командную строку** (cmd), а не PowerShell
2. Перейдите в директорию проекта:
   ```
   cd "C:\Users\jilig\PycharmProjects\pythonProject\Парсер PERPLEX"
   ```
3. Активируйте виртуальное окружение:
   ```
   .venv\Scripts\activate
   ```
4. Запустите сервер:
   ```
   python server.py
   ```

#### Способ 2: Через PyCharm

1. Откройте проект в PyCharm
2. Откройте файл `server.py`
3. Нажмите правой кнопкой на файл → "Run 'server'"

#### Способ 3: Через проводник

1. Откройте проводник
2. Перейдите в папку проекта
3. В адресной строке введите `cmd` и нажмите Enter
4. Выполните:
   ```
   .venv\Scripts\activate
   python server.py
   ```

### 4. Проверьте, что сервер запустился

Вы должны увидеть:
```
============================================================
🚀 Запуск веб-сервера системы конкурентного анализа
============================================================
📂 Рабочая директория: C:\Users\jilig\...
✅ Все необходимые файлы найдены
📍 Адрес: http://localhost:5000
============================================================
✅ Все модули импортированы успешно

🟢 Сервер запускается...
```

### 5. Откройте браузер

Перейдите по адресу: **http://localhost:5000**

### Если сервер не запускается

1. **Проверьте виртуальное окружение:**
   ```powershell
   .venv\Scripts\activate
   pip list
   ```
   Убедитесь, что установлены все зависимости из `requirements.txt`

2. **Установите зависимости заново:**
   ```powershell
   pip install -r requirements.txt
   ```

3. **Проверьте логи:**
   Ошибки будут видны в консоли, где запущен сервер

### Частые проблемы

- **Черный экран в браузере:** Сервер не запущен или упал. Перезапустите сервер.
- **Ошибка "Address already in use":** Порт 5000 занят. Остановите другие процессы.
- **Ошибка импорта модулей:** Активируйте виртуальное окружение и установите зависимости.

