# 📚 Полная документация системы конкурентного анализа

## 🎯 Общее описание

Система конкурентного анализа предназначена для:
1. Загрузки каталога товаров из 1С (Excel/XML)
2. Поиска аналогичных товаров на маркетплейсах (Wildberries, Ozon, Avito, Яндекс.Маркет, Mr-moto.ru)
3. Сопоставления товаров с использованием алгоритмов нечеткого поиска
4. Генерации отчетов с анализом цен конкурентов

---

## 🏗️ Архитектура системы

### Компоненты системы:

```
┌─────────────────┐
│   index.html    │  ← Веб-интерфейс
│   app.js        │  ← Логика фронтенда
│   style.css     │  ← Стили
└────────┬────────┘
         │ HTTP/WebSocket
         ▼
┌─────────────────┐
│   server.py     │  ← Flask сервер + SocketIO
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ main_system.py  │  ← Основная логика
└────────┬────────┘
         │
    ┌────┴────┬──────────────┬─────────────┐
    ▼         ▼              ▼             ▼
┌────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│Scraper │ │CommerceML│ │Product   │ │parse_1c_ │
│Manager │ │Parser    │ │Matcher   │ │improved  │
└────────┘ └──────────┘ └──────────┘ └──────────┘
```

---

## 📖 Подробное описание работы системы

### 1️⃣ Загрузка каталога из 1С

**Файл:** `main_system.py` → `load_catalog_from_1c()`

**Процесс:**
1. Пользователь загружает файл через веб-интерфейс
2. Система определяет формат файла (`.xlsx`, `.xls`, `.xml`)
3. В зависимости от формата:
   - **Excel** → используется `parse_1c_improved.py`
   - **XML** → используется `commerceml_parser.py`
4. Извлекаются данные о товарах:
   - Название
   - Артикул (ID)
   - Цена
   - Остаток (если есть)

**Результат:** Список товаров сохраняется в `self.products_1c`

---

### 2️⃣ Парсинг маркетплейсов

**Файл:** `main_system.py` → `scrape_competitors()`

**Процесс:**
1. Для каждого товара из 1С (ограничено `max_products_from_1c`):
   - Берется название товара как поисковый запрос
   - Запрос отправляется в `ScraperManager`
   
2. **ScraperManager** (`scrapers/scraper_manager.py`):
   - Определяет, какие сайты нужно парсить
   - Для каждого сайта вызывает соответствующий скрапер:
     - `wildberries_scraper.py`
     - `ozon_scraper.py`
     - `avito_scraper.py`
     - `yandex_market_scraper.py`
     - `universal_scraper.py` (для других сайтов)
   
3. **Каждый скрапер:**
   - Открывает браузер (Selenium + undetected-chromedriver)
   - Переходит на страницу поиска маркетплейса
   - Вводит поисковый запрос
   - Парсит результаты поиска:
     - Название товара
     - Цену
     - Ссылку на товар
   - Возвращает список `ScrapedProduct`

4. **Кэширование браузеров:**
   - Браузер для каждого сайта открывается один раз
   - Переиспользуется для всех поисковых запросов
   - Это значительно ускоряет парсинг

**Результат:** Список найденных товаров сохраняется в `self.scraped_products`

---

### 3️⃣ Сопоставление товаров (MATCHING)

**Файл:** `product_matcher.py` → `match_products()`

Это **самая важная** часть системы! Здесь происходит интеллектуальное сопоставление.

#### 🔍 Как работает алгоритм сопоставления:

**Шаг 1: Предобработка названий**

Для каждого товара из 1С и каждого найденного товара:
- Приведение к нижнему регистру
- Удаление специальных символов
- Нормализация пробелов
- Замена общих слов:
  - "мотошлем" → "шлем"
  - "helmet" → "шлем"
  - "размер" → "" (удаляется)
  - "цвет" → "" (удаляется)

**Пример:**
```
Исходное: "Мотошлем HJC RPHA71 CARBON XL"
После обработки: "шлем hjc rpha71 carbon xl"
```

**Шаг 2: Вычисление схожести**

Используется **несколько алгоритмов** одновременно:

1. **SequenceMatcher (Levenshtein)**
   - Сравнивает строки посимвольно
   - Вычисляет минимальное количество изменений для превращения одной строки в другую
   - Возвращает значение от 0.0 до 1.0

2. **Token Similarity (Токенное сходство)**
   - Разбивает названия на слова (токены)
   - Считает пересечение множеств токенов
   - Формула: `пересечение / объединение`

3. **FuzzyWuzzy Ratio**
   - Использует библиотеку `fuzzywuzzy`
   - Комбинирует несколько метрик схожести
   - Более точный для похожих названий

**Пример вычисления:**
```python
Название 1С: "Гайка М16х1.5 фитинг"
Название WB: "Гайка М16 фитинг"

SequenceMatcher: 0.75
Token Similarity: 0.80 (4 общих слова из 5)
FuzzyWuzzy: 0.82

Итоговый score: max(0.75, 0.80, 0.82) = 0.82
```

**Шаг 3: Взвешенная оценка**

Система учитывает не только название, но и другие параметры:

```python
weights = {
    "name": 0.6,      # 60% - название товара
    "brand": 0.2,     # 20% - бренд
    "size": 0.15,     # 15% - размер
    "category": 0.05  # 5% - категория
}
```

**Итоговая схожесть:**
```python
total_score = (
    name_similarity * 0.6 +
    brand_similarity * 0.2 +
    size_similarity * 0.15 +
    category_similarity * 0.05
)
```

**Шаг 4: Применение порога**

**Порог совпадения (threshold)** - это минимальный процент схожести, при котором товары считаются совпадающими.

**Как работает порог:**

```python
if match.similarity_score >= threshold:
    # Товары считаются совпадающими
    matches.append(match)
else:
    # Товары НЕ совпадают
    # Не добавляется в результаты
```

**Примеры:**

| Порог | Что найдет | Когда использовать |
|-------|-----------|-------------------|
| **50-60%** | Очень похожие товары + много "мусора" | Для широкого поиска |
| **60-70%** | Похожие товары (рекомендуется) | Стандартные товары |
| **70-80%** | Очень похожие товары | Брендовые товары |
| **85-90%** | Почти идентичные названия | Точные совпадения |
| **90-100%** | Только точные копии | Очень специфичные товары |

**Важно:** Порог задается в диапазоне **0.0 - 1.0**, но в интерфейсе отображается в процентах (0-100%).

**Пример:**
- Порог **70%** = `threshold = 0.70`
- Порог **85%** = `threshold = 0.85`

**Шаг 5: Уровни уверенности**

После сопоставления каждое совпадение получает уровень уверенности:

```python
confidence_levels = {
    "high": 0.9,    # ≥ 90% - высокая уверенность
    "medium": 0.7,  # ≥ 70% - средняя уверенность
    "low": 0.5      # ≥ 50% - низкая уверенность
}
```

**Пример:**
- Схожесть **92%** → `confidence = "high"`
- Схожесть **75%** → `confidence = "medium"`
- Схожесть **65%** → `confidence = "low"`

**Шаг 6: Расчет разницы цен**

Для каждого совпадения вычисляется:
- `price_difference = price_scraped - price_1c`
- `price_difference_percent = (price_difference / price_1c) * 100`

**Пример:**
```
Цена 1С: 500 руб
Цена конкурента: 600 руб

Разница: +100 руб (+20%)
```

---

### 4️⃣ Генерация отчетов

**Файл:** `main_system.py` → `generate_report()`

**Форматы отчетов:**

#### JSON (`format='json'`)
- Полная информация о всех товарах
- Все совпадения с деталями
- Статистика

#### CSV (`format='csv'`)
- Таблица для Excel
- Разделитель: `;` (точка с запятой)
- Кодировка: `UTF-8-sig` (с BOM для Excel)
- Все поля в кавычках

**Структура CSV:**
```csv
Товар 1С;Артикул;Цена 1С (руб);Маркетплейс;Название на маркетплейсе;Цена конкурента (руб);Разница (руб);Разница (%);Совпадение (%);Ссылка
```

#### Excel (`format='excel'`)
- Несколько листов:
  - "Совпадения" - все найденные совпадения
  - "Товары 1С" - исходный каталог
  - "Спарсено" - все найденные товары

---

## 🔧 Конфигурация

### `matching_config.json`

Настройки алгоритма сопоставления:

```json
{
  "threshold": 0.85,        // Порог по умолчанию (85%)
  "weights": {
    "name": 0.6,          // Вес названия
    "brand": 0.2,          // Вес бренда
    "size": 0.15,          // Вес размера
    "category": 0.05       // Вес категории
  },
  "algorithms": [
    "levenshtein",         // Алгоритм Левенштейна
    "token_similarity",    // Токенное сходство
    "fuzzy_ratio"         // FuzzyWuzzy
  ],
  "confidence_levels": {
    "high": 0.9,          // Высокая уверенность
    "medium": 0.7,        // Средняя уверенность
    "low": 0.5            // Низкая уверенность
  }
}
```

---

## 📊 Пример работы системы

### Входные данные:

**Товар из 1С:**
```json
{
  "id": "HJC-RPHA71-XL",
  "name": "Мотошлем HJC RPHA71 CARBON XL",
  "price": 63000
}
```

**Найденные товары на Wildberries:**
```json
[
  {
    "title": "HJC RPHA71 CARBON CARBON XL",
    "price": 63000,
    "url": "https://www.wildberries.ru/...",
    "source": "wildberries"
  },
  {
    "title": "Шлем RPHA71 MATTE BLACK HJC XL",
    "price": 41690,
    "url": "https://www.wildberries.ru/...",
    "source": "wildberries"
  }
]
```

### Процесс сопоставления:

**1. Предобработка:**
```
1С: "мотошлем hjc rpha71 carbon xl"
WB1: "hjc rpha71 carbon carbon xl"
WB2: "шлем rpha71 matte black hjc xl"
```

**2. Вычисление схожести:**

Для WB1:
- SequenceMatcher: 0.85
- Token Similarity: 0.90
- FuzzyWuzzy: 0.88
- **Итог: 0.90**

Для WB2:
- SequenceMatcher: 0.70
- Token Similarity: 0.75
- FuzzyWuzzy: 0.72
- **Итог: 0.75**

**3. Применение порога (70%):**

- WB1: 0.90 ≥ 0.70 ✅ **Совпадение!**
- WB2: 0.75 ≥ 0.70 ✅ **Совпадение!**

**4. Результат:**

```json
[
  {
    "product_1c_name": "Мотошлем HJC RPHA71 CARBON XL",
    "scraped_product_title": "HJC RPHA71 CARBON CARBON XL",
    "similarity_score": 0.90,
    "price_1c": 63000,
    "price_scraped": 63000,
    "price_difference": 0,
    "price_difference_percent": 0.0,
    "confidence": "high"
  },
  {
    "product_1c_name": "Мотошлем HJC RPHA71 CARBON XL",
    "scraped_product_title": "Шлем RPHA71 MATTE BLACK HJC XL",
    "similarity_score": 0.75,
    "price_1c": 63000,
    "price_scraped": 41690,
    "price_difference": -21310,
    "price_difference_percent": -33.8,
    "confidence": "medium"
  }
]
```

---

## 🎯 Рекомендации по настройке порога

### Для разных типов товаров:

**Стандартные товары (гайки, болты, фитинги):**
- Порог: **60-70%**
- Причина: Много вариаций названий, разные форматы

**Брендовые товары (HEL, Bajaj, HJC):**
- Порог: **70-80%**
- Причина: Более стандартизированные названия

**Уникальные артикулы:**
- Порог: **50-60%**
- Причина: Много различий в написании

### Как подобрать оптимальный порог:

1. **Начните с 70%**
2. Запустите анализ на 5-10 товарах
3. Посмотрите результаты:
   - Если слишком много "мусора" → увеличьте до 75-80%
   - Если ничего не находит → снизьте до 60-65%
4. Проверьте несколько совпадений вручную (откройте ссылки)
5. Скорректируйте порог

---

## 🔄 Поток данных

```
1. Пользователь загружает файл
   ↓
2. main_system.load_catalog_from_1c()
   → parse_1c_improved.parse() или commerceml_parser.parse()
   → self.products_1c = [...]
   ↓
3. Пользователь запускает анализ
   ↓
4. main_system.scrape_competitors()
   → Для каждого товара:
     → scraper_manager.search_all()
       → Для каждого сайта:
         → wildberries_scraper.search() / ozon_scraper.search() / ...
         → Возвращает ScrapedProduct[]
   → self.scraped_products = [...]
   ↓
5. main_system.match_products(threshold)
   → product_matcher.match_products()
     → Для каждого товара 1С:
       → _find_best_matches()
         → Для каждого найденного товара:
           → _calculate_similarity()
             → _compare_texts() (несколько алгоритмов)
             → Взвешенная сумма
           → if similarity >= threshold:
               → Добавить в matches
   → self.matches = [...]
   ↓
6. main_system.generate_report('csv')
   → Создает CSV файл
   → Возвращает путь к файлу
   ↓
7. Пользователь скачивает отчет
```

---

## 📁 Структура файлов

### Основные файлы:

- `server.py` - Flask сервер, API endpoints
- `main_system.py` - Основная логика системы
- `product_matcher.py` - Алгоритм сопоставления
- `commerceml_parser.py` - Парсинг XML из 1С
- `parse_1c_improved.py` - Парсинг Excel из 1С

### Веб-интерфейс:

- `index.html` - HTML структура
- `app.js` - JavaScript логика
- `style.css` - Стили

### Скраперы:

- `scrapers/scraper_manager.py` - Менеджер скраперов
- `scrapers/wildberries_scraper.py` - Парсер Wildberries
- `scrapers/ozon_scraper.py` - Парсер Ozon
- `scrapers/avito_scraper.py` - Парсер Avito
- `scrapers/yandex_market_scraper.py` - Парсер Яндекс.Маркет
- `scrapers/universal_scraper.py` - Универсальный парсер

### Конфигурация:

- `matching_config.json` - Настройки сопоставления
- `scraper_config.json` - Настройки скраперов
- `requirements.txt` - Python зависимости

### Скрипты запуска:

- `start_server.bat` - Запуск сервера
- `stop_server.bat` - Остановка сервера
- `check_server.bat` - Проверка статуса
- `restart_server.bat` - Перезапуск сервера

---

## 🚀 Запуск системы

1. Активируйте виртуальное окружение:
   ```cmd
   .venv\Scripts\activate
   ```

2. Запустите сервер:
   ```cmd
   start_server.bat
   ```
   или
   ```cmd
   python server.py
   ```

3. Откройте браузер:
   ```
   http://localhost:5000
   ```

---

## 📝 Логирование

Система логирует все важные события:

- Загрузка файлов
- Парсинг товаров
- Поиск на маркетплейсах
- Сопоставление товаров
- Генерация отчетов

Логи сохраняются в `data/logs/system_YYYYMMDD.log`

---

## ⚠️ Важные замечания

1. **Порог совпадения** - это не точность, а фильтр. Чем выше порог, тем меньше совпадений, но они точнее.

2. **Производительность:**
   - Парсинг 1 товара = 10-30 секунд
   - 10 товаров = 3-5 минут
   - 25 товаров = 8-12 минут

3. **Кэширование браузеров:**
   - Браузеры переиспользуются между запросами
   - Это ускоряет парсинг в 3-5 раз

4. **Ограничения:**
   - Некоторые сайты могут блокировать частые запросы
   - Рекомендуется парсить не более 25 товаров за раз

---

## 🎓 Глоссарий

- **Порог совпадения (threshold)** - минимальный процент схожести для признания товаров совпадающими (0.0 - 1.0)
- **Схожесть (similarity)** - числовая оценка похожести двух названий (0.0 - 1.0)
- **Уверенность (confidence)** - уровень уверенности в совпадении (high/medium/low)
- **Fuzzy matching** - нечеткий поиск, поиск похожих, но не идентичных строк
- **Scraping** - парсинг, извлечение данных с веб-страниц
- **Headless** - режим браузера без графического интерфейса

---

**Версия документации:** 1.0  
**Дата обновления:** 2025-11-08

